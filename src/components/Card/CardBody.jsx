import { generateSingleElementComponent } from "../../helpers/generateSingleElementComponent";

const params = {
  className: "card-body",
  as: "div",
};

export const CardBody = generateSingleElementComponent(params);
